"use client";

import React, { useEffect, useState } from "react";
import StreamsComposer from "../current-chat/new-face/composer/StreamsComposer";
import ChatMarkdownMessage from "../current-chat/new-face/markdown/ChatMarkdownMessage";

const NAV_GROUPS = [
  { title: "MAIN", items: [["home", "Home"], ["portfolio", "Portfolio"], ["projects", "Projects"]] },
  { title: "BUILD", items: [["business", "Business Builder"], ["revenue", "Revenue"], ["visuals", "Visual Concepts"], ["website", "Website Builder"], ["app", "App Builder"], ["preview-launch", "Preview + Launch"]] },
  { title: "CREATE", items: [["studio", "Creator Studio"], ["image", "Image Studio"], ["video", "Video Studio"], ["voice", "Voice Studio"], ["captions", "Captions"], ["content", "Content"], ["ideas", "Ideas"], ["turn-you", "Turn This Into You"], ["calendar", "Calendar"], ["social", "Social Research"], ["growth-feed", "Growth Feed"], ["assets", "Assets"]] },
  { title: "GROW", items: [["checklist", "Launch Checklist"], ["growth", "Growth Dashboard"], ["notifications", "Notifications"]] },
  { title: "ACCOUNT", items: [["billing", "Billing / Credits"], ["notification-settings", "Notification Settings"], ["settings", "Settings"]] },
];
const MOBILE_TABS = [["home", "Home"], ["portfolio", "Portfolio"], ["create", "Create"], ["preview-launch", "Launch"], ["settings", "Profile"]];
const PREVIEW_MODES = ["Concept", "Marketing", "Artifact", "Progress"];

function titleOf(id) { return NAV_GROUPS.flatMap((g) => g.items).find(([key]) => key === id)?.[1] || "Streams AI"; }
function cleanText(value) { return String(value || "").replace(/\s+/g, " ").trim(); }
function buildSessionTitle(session, index) { return cleanText(session?.title || session?.name || session?.id) || `Project ${index + 1}`; }
function getMessageText(message) { return message?.content || message?.text || ""; }
function useIsMobile() { const [isMobile, setIsMobile] = useState(false); useEffect(() => { const update = () => setIsMobile(window.innerWidth < 900); update(); window.addEventListener("resize", update); return () => window.removeEventListener("resize", update); }, []); return isMobile; }

function DesktopSidebar({ activeSection, setActiveSection, onNewChat }) {
  return <aside className="streamsOpSidebar" aria-label="Streams AI navigation"><button type="button" className="newSession" onClick={onNewChat}>+ New session</button><div className="navList">{NAV_GROUPS.map((group) => <section key={group.title}><h3>{group.title}</h3>{group.items.map(([id, label]) => <button type="button" key={id} className={activeSection === id ? "active" : ""} onClick={() => setActiveSection(id)}>{label}</button>)}</section>)}</div></aside>;
}

function ChatMessage({ message }) { const isUser = message.role === "user"; const text = getMessageText(message); return <article className={isUser ? "msg user" : "msg assistant"}>{!isUser && <div className="avatar">AI</div>}<div className="bubble">{message.attachments?.length ? <div className="attachments">{message.attachments.map((asset) => <span key={asset.id || asset.name}>{asset.name || asset.kind || "Attachment"}</span>)}</div> : null}{message.generatedImage?.url ? <img className="generated" src={message.generatedImage.url} alt={message.generatedImage.name || "Generated image"}/> : message.generatedVideoUrl ? <video className="generated" src={message.generatedVideoUrl} controls playsInline/> : <ChatMarkdownMessage content={text}/>}</div></article>; }
function ChatPanel({ chatRuntime, activeProject, onOpenInline }) { const messages = Array.isArray(chatRuntime?.messages) ? chatRuntime.messages : []; const hasMessages = messages.length > 0; return <section className="chatPanel"><div className="microbar"><div><span>STREAMS AI</span><b>{activeProject ? activeProject.title : "General assistant"}</b></div><div><small>{chatRuntime?.isStreaming ? "Working" : "Online"}</small><button type="button" disabled={!activeProject} onClick={onOpenInline}>Inline Build</button></div></div><div className="chatScroll">{!hasMessages ? <div className="empty"><div className="orb"/><h1>Ask, build, create, launch.</h1><p>Chat is ready. Open Portfolio when you want project memory and inline visual build.</p></div> : messages.map((message) => <ChatMessage key={message.id || `${message.role}-${message.createdAt}`} message={message}/>)}</div><div className="composer"><StreamsComposer onSubmit={(payload) => chatRuntime?.sendMessage?.({ message: payload.message, composerMode: payload.composerMode, mode: payload.mode, webSearchEnabled: payload.webSearchEnabled })} onFilesSelected={(files) => chatRuntime?.uploadFiles?.(files)} onProviderChange={(provider) => chatRuntime?.setSelectedProvider?.(provider)} onModeChange={(mode) => chatRuntime?.setSelectedMode?.(mode)} libraryFiles={chatRuntime?.composerAttachments || []} onRemoveFile={(fileId) => chatRuntime?.removeComposerAttachment?.(fileId)} isStreaming={chatRuntime?.isStreaming}/></div></section>; }
function ProjectPicker({ sessions, activeProject, onSelectProject, onStartCleanProject }) { const recent = (Array.isArray(sessions) ? sessions : []).slice(0, 12); return <section className="module"><span>PORTFOLIO</span><h1>Open a brand or project</h1><p>No project auto-opens. Selecting a project loads only that project’s isolated memory, assets, preview state, and launch context.</p><div className="actions"><button type="button" onClick={onStartCleanProject}>Start clean project</button><button type="button" onClick={() => onSelectProject(null)}>General mode</button></div>{recent.length ? <div className="grid">{recent.map((session, index) => { const title = buildSessionTitle(session, index); const id = session?.id || session?.sessionId || title; const selected = activeProject?.id === id; return <button key={id} type="button" className={selected ? "project active" : "project"} onClick={() => onSelectProject({ id, title, source: "chat-session" })}><b>{title}</b><em>{selected ? "Active project" : "Open isolated memory"}</em></button>; })}</div> : <div className="emptyBox">No saved sessions found. Start clean or stay in general mode.</div>}</section>; }
function InlineBuildPanel({ activeProject, inlineOpen, onClose }) { const [mode, setMode] = useState("Concept"); if (!inlineOpen) return null; return <aside className="inlinePanel"><div className="inlineHead"><div><span>INLINE BUILD</span><h2>{activeProject?.title || "No project selected"}</h2></div><button type="button" onClick={onClose}>×</button></div><div className="modeTabs">{PREVIEW_MODES.map((item) => <button key={item} type="button" className={mode === item ? "active" : ""} onClick={() => setMode(item)}>{item}</button>)}</div><div className="previewHero"><span>{mode}</span><h3>{activeProject ? "Visual build surface ready" : "Choose a project first"}</h3><p>Live visuals, samples, artifacts, and progress appear here only when connected to real project outputs.</p></div><div className="income"><b>Income Potential — AI Estimate</b><span>Starter: --</span><span>Growth: --</span><span>Scale: --</span></div></aside>; }
function ModuleScreen({ id, activeProject }) { return <section className="module"><span>{activeProject ? "PROJECT" : "GENERAL"}</span><h1>{titleOf(id)}</h1><p>This page is available in the Streams AI navigation. It must connect to real saved data before showing completed outputs.</p><div className="cards"><div><b>Status</b><em>Ready</em></div><div><b>Rule</b><em>No fake outputs</em></div><div><b>Memory</b><em>{activeProject ? "Selected project only" : "No project loaded"}</em></div></div></section>; }
function MainContent({ activeSection, chatRuntime, activeProject, onSelectProject, onStartCleanProject, onOpenInline }) { if (activeSection === "home") return <ChatPanel chatRuntime={chatRuntime} activeProject={activeProject} onOpenInline={onOpenInline}/>; if (activeSection === "portfolio" || activeSection === "projects") return <ProjectPicker sessions={chatRuntime?.sessions || []} activeProject={activeProject} onSelectProject={onSelectProject} onStartCleanProject={onStartCleanProject}/>; return <ModuleScreen id={activeSection} activeProject={activeProject}/>; }
function MobileBottomNav({ activeSection, setActiveSection }) { return <nav className="mobileNav">{MOBILE_TABS.map(([id, label]) => <button key={id} type="button" className={activeSection === id ? "active" : ""} onClick={() => setActiveSection(id)}>{label}</button>)}</nav>; }
export default function StreamsOperatorShell({ chatRuntime }) { const isMobile = useIsMobile(); const [activeSection, setActiveSection] = useState("home"); const [activeProject, setActiveProject] = useState(null); const [inlineOpen, setInlineOpen] = useState(false); useEffect(() => setInlineOpen(false), [activeProject?.id]); const startCleanProject = () => { chatRuntime?.newChat?.(); setActiveProject({ id: `draft-${Date.now()}`, title: "New clean project", source: "draft" }); setActiveSection("home"); }; const selectProject = (project) => { setActiveProject(project); setInlineOpen(false); setActiveSection("home"); if (project?.source === "chat-session" && project.id) chatRuntime?.selectSession?.(project.id); if (!project) chatRuntime?.newChat?.(); }; const newGeneralChat = () => { setActiveProject(null); setInlineOpen(false); setActiveSection("home"); chatRuntime?.newChat?.(); }; const shellClass = ["shell", inlineOpen ? "inlineOpen" : "", isMobile ? "mobile" : "desktop"].filter(Boolean).join(" "); return <div className={shellClass}><style>{styles}</style>{!isMobile ? <DesktopSidebar activeSection={activeSection} setActiveSection={setActiveSection} onNewChat={newGeneralChat}/> : null}<main><MainContent activeSection={activeSection} chatRuntime={chatRuntime} activeProject={activeProject} onSelectProject={selectProject} onStartCleanProject={startCleanProject} onOpenInline={() => activeProject && setInlineOpen(true)}/></main><InlineBuildPanel activeProject={activeProject} inlineOpen={inlineOpen} onClose={() => setInlineOpen(false)}/>{isMobile ? <MobileBottomNav activeSection={activeSection} setActiveSection={setActiveSection}/> : null}</div>; }

const styles = `
.shell{height:100dvh;display:grid;grid-template-columns:160px minmax(0,1fr);background:#02050c;color:#eff6ff;font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;overflow:hidden}.shell.inlineOpen{grid-template-columns:160px minmax(0,1fr) 400px}.streamsOpSidebar{width:160px;background:#070b1d;display:flex;flex-direction:column;overflow:hidden;border-right:1px solid rgba(56,127,255,.22)}.newSession{height:21px;border:0;background:transparent;color:#fff;font-size:10.5px;font-weight:800;text-align:left;padding:0 6px}.navList{flex:1;overflow:hidden;padding:2px 0}.navList section{margin:0 0 5px}.navList h3{margin:0 0 1px 5px;color:#80a1d5;font-size:8px;font-weight:900;letter-spacing:.19em}.navList button{width:100%;height:19px;border:0;background:transparent;color:#eaf2ff;text-align:left;border-radius:0;padding:0 5px;font-weight:700;font-size:11px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.navList button:hover,.navList button.active{background:linear-gradient(90deg,rgba(34,201,255,.18),rgba(34,201,255,.08));box-shadow:inset 0 0 0 1px rgba(89,190,255,.20)}main{height:100dvh;min-width:0;background:linear-gradient(180deg,#030815 0%,#02050c 100%);position:relative;overflow:hidden}main:before{content:"";position:absolute;inset:0;background:linear-gradient(180deg,rgba(16,43,84,.42),rgba(0,0,0,0) 42%),radial-gradient(circle at 92% 33%,rgba(122,29,255,.18),transparent 12%);pointer-events:none}.chatPanel{position:relative;z-index:1;height:100%;display:grid;grid-template-rows:minmax(0,1fr) auto;background:transparent;color:#f4f8ff;overflow:hidden}.microbar{position:absolute;left:28px;right:28px;top:18px;z-index:4;display:flex;align-items:center;justify-content:space-between;pointer-events:none}.microbar span{display:block;color:#37e5ff;font-size:10px;font-weight:900;letter-spacing:.22em}.microbar b{display:block;margin-top:4px;font-size:12px;color:#aac2ee;font-weight:650}.microbar div:last-child{display:flex;gap:8px;align-items:center}.microbar small{border:1px solid rgba(56,221,255,.28);background:rgba(24,214,255,.10);border-radius:999px;color:#bdf6ff;padding:6px 9px;font-weight:800}.microbar button{pointer-events:auto;border:1px solid rgba(148,163,184,.18);background:rgba(255,255,255,.05);color:#d7e5ff;border-radius:999px;height:28px;padding:0 11px;font-weight:800}.microbar button:disabled{opacity:.35}.chatScroll{min-height:0;overflow:auto;padding:76px 32px 160px;display:flex;flex-direction:column;gap:18px}.empty{margin:auto;text-align:center;max-width:760px}.orb{width:88px;height:88px;margin:0 auto 24px;border-radius:28px;background:radial-gradient(circle,#27e8ff 0 8%,#4874ff 22%,#8e55ff 56%,#f33fc3 100%);box-shadow:0 0 80px rgba(38,216,255,.35)}.empty h1{font-size:52px;font-weight:400;margin:0 0 22px;color:#fff}.empty p{font-size:24px;line-height:1.45;color:#b7cdf7;margin:0}.msg{display:grid;grid-template-columns:42px minmax(0,1fr);gap:12px;max-width:980px}.msg.user{margin-left:auto;grid-template-columns:minmax(0,1fr)}.avatar{width:38px;height:38px;border-radius:14px;display:grid;place-items:center;background:#111a38;color:#78e4ff;font-size:12px;font-weight:900;border:1px solid rgba(45,212,255,.18)}.bubble{background:rgba(10,17,38,.86);border:1px solid rgba(99,131,189,.26);border-radius:24px 24px 24px 5px;padding:16px 18px;line-height:1.68;color:#eef6ff;font-size:18px}.msg.user .bubble{background:linear-gradient(135deg,#121936,#0b1023);color:#fff!important;border-color:rgba(45,212,255,.22);border-radius:24px 24px 5px 24px}.msg.user .bubble *{color:#fff!important}.bubble p{margin:0 0 12px}.attachments{display:flex;flex-wrap:wrap;gap:6px;margin-bottom:8px}.attachments span{background:rgba(255,255,255,.09);border-radius:999px;padding:4px 8px;font-size:12px}.generated{width:min(100%,420px);border-radius:18px;display:block;background:#000}.composer{position:absolute;left:72px;right:72px;bottom:18px;z-index:5;background:transparent}.composer small{display:none}.composer :is(input,textarea){color:#fff!important}.module{height:100%;overflow:auto;padding:78px 38px 38px;color:#fff}.module span{display:block;color:#37e5ff;font-size:10px;font-weight:900;letter-spacing:.22em}.module h1{font-size:34px;margin:5px 0 8px}.module p{color:#a8b9da;line-height:1.55}.actions{display:flex;gap:10px;flex-wrap:wrap;margin:18px 0}.actions button{border:0;border-radius:10px;min-height:34px;padding:0 12px;background:linear-gradient(135deg,#20d2ff,#7357ff);font-weight:800}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:10px}.project{border:1px solid rgba(148,163,184,.18);background:rgba(255,255,255,.05);color:#edf6ff;text-align:left;border-radius:18px;padding:14px;min-height:86px}.project b,.project em{display:block}.project em{font-style:normal;margin-top:8px;color:#9eb0d0;font-size:12px}.project.active{border-color:#32d8ff}.emptyBox{color:#91a4c5;border:1px dashed rgba(148,163,184,.2);border-radius:16px;padding:16px}.cards{display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:12px;margin-top:20px}.cards div{border:1px solid rgba(148,163,184,.18);background:rgba(255,255,255,.05);border-radius:18px;padding:16px}.cards b,.cards em{display:block}.cards em{font-style:normal;margin-top:8px;color:#a6b9dc}.inlinePanel{height:100dvh;overflow:auto;border-left:1px solid rgba(65,199,255,.18);background:linear-gradient(180deg,#090d1b,#0e1430);padding:18px}.inlineHead{display:flex;justify-content:space-between;gap:12px}.inlineHead span{color:#37e5ff;font-size:10px;font-weight:900;letter-spacing:.22em}.inlineHead h2{font-size:23px;margin:4px 0 14px}.inlineHead button{width:38px;height:38px;border:1px solid rgba(255,255,255,.12);border-radius:14px;background:rgba(255,255,255,.06);color:#fff;font-size:24px}.modeTabs{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;margin-bottom:14px}.modeTabs button{border:1px solid rgba(148,163,184,.18);background:rgba(255,255,255,.05);color:#dce9ff;border-radius:14px;min-height:38px}.modeTabs button.active{background:linear-gradient(135deg,#12d6ff,#7a5cff);color:#06101d;font-weight:900}.previewHero{min-height:260px;border:1px solid rgba(45,212,255,.22);border-radius:28px;background:radial-gradient(circle at 50% 30%,rgba(51,214,255,.28),transparent 28%),linear-gradient(135deg,rgba(36,50,96,.95),rgba(8,11,24,.95));padding:22px;display:flex;flex-direction:column;justify-content:end}.previewHero span{color:#7df0ff;font-weight:900;font-size:12px;letter-spacing:.14em}.previewHero h3{font-size:27px;margin:8px 0 6px}.previewHero p{color:#9eb0d0;line-height:1.55}.income{margin-top:12px;border:1px solid rgba(45,212,255,.18);background:rgba(255,255,255,.06);border-radius:18px;padding:12px;display:grid;gap:6px;font-size:13px}.income b{color:#fff}.income span{color:#aecaef}.mobileNav{display:none}@media (max-width:899px){.shell,.shell.inlineOpen{display:flex;flex-direction:column;height:100dvh}.streamsOpSidebar{display:none}main{height:auto;flex:1}.microbar{left:14px;right:14px;top:12px}.chatScroll{padding:68px 14px 130px}.empty h1{font-size:28px}.empty p{font-size:16px}.composer{left:10px;right:10px;bottom:76px}.module{padding:68px 18px 110px}.inlinePanel{position:fixed;left:0;right:0;top:0;bottom:74px;height:auto;z-index:80}.mobileNav{position:fixed;left:0;right:0;bottom:0;z-index:90;display:grid;grid-template-columns:repeat(5,1fr);gap:4px;padding:8px 8px calc(8px + env(safe-area-inset-bottom,0px));background:rgba(8,11,24,.96);border-top:1px solid rgba(148,163,184,.18)}.mobileNav button{border:0;background:transparent;color:#91a4c5;border-radius:14px;min-height:44px;font-size:12px;font-weight:800}.mobileNav button.active{background:rgba(45,212,255,.14);color:#fff}.msg{max-width:100%;grid-template-columns:32px minmax(0,1fr)}.msg.user{grid-template-columns:minmax(0,1fr)}.avatar{width:32px;height:32px}.bubble{padding:12px 13px;font-size:16px}}
`;
