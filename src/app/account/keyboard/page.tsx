import StreamsAccountActionPanel from "@/components/account/StreamsAccountActionPanel";

export default function AccountKeyboardPage() {
  return (
    <StreamsAccountActionPanel
      pageKind="keyboard"
      title="Keyboard"
      description="Review shortcut readiness, productivity controls, and account-level keyboard preferences."
    />
  );
}
