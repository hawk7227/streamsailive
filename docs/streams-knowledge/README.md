# STREAMS Knowledge Packs

Task-scoped routing docs for the STREAMS General Builder Runtime retrieval layer. These do not replace source-of-truth code.

The runtime is project-agnostic. STREAMS self-build is the first/default project profile, not the only supported runtime mode.
