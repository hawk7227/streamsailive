# STREAMS General Builder Runtime
- OpenAI = reasoning provider.
- STREAMS = context retrieval, execution, policy guardrails, proof.
- Core runtime is project/profile agnostic.
- STREAMS self-build is one profile (`streams_self_build`) in this general runtime.
- Missing capabilities must return Blocked with exact reason.
